# 사용자로부터 두 개의 숫자를 입력받음
A, B = map(int, input().split())

# 입력된 숫자가 1 이상 100,000 이하의 범위에 속하는지 확인
if 1 <= A and B <= 100000:
    # 범위에 속하면 (A + B) * (A - B)의 결과를 출력
    print((A + B) * (A - B))
else:
    # 범위에 속하지 않으면 메시지를 출력
    print('1 ≤ A, B ≤ 100,000 범위의 숫자를 입력해주세요')
